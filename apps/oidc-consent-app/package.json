{
  "name": "oidc-consent-app",
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target bun",
    "check": "bun x tsc --noEmit",
    "dev": "bun run --hot src/index.ts"
  },
  "dependencies": {
    "@hono/standard-validator": "^0.2.2",
    "@saurbit/hono-oauth2": "^0.1.2",
    "@saurbit/oauth2": "^0.1.3",
    "@saurbit/oauth2-jwt": "0.1.3",
    "@scalar/hono-api-reference": "^0.10.7",
    "hono": "^4.12.12",
    "hono-openapi": "^1.3.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12"
  }
}