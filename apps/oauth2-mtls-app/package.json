{
  "name": "oauth2-mtls-app",
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target bun",
    "check": "tsc --noEmit",
    "dev": "bun run --hot src/index.ts"
  },
  "dependencies": {
    "@saurbit/hono-oauth2": "^0.1.10",
    "@saurbit/oauth2": "^0.1.16",
    "@saurbit/oauth2-jwt": "^0.1.11",
    "hono": "^4.13.7"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0"
  }
}