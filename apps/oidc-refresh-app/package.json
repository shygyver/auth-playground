{
  "name": "oidc-refresh-app",
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target bun",
    "check": "tsc --noEmit",
    "dev": "bun run --hot src/index.ts"
  },
  "dependencies": {
    "@hono/standard-validator": "^0.2.2",
    "@saurbit/hono-oauth2": "^0.1.10",
    "@saurbit/oauth2": "^0.1.16",
    "@saurbit/oauth2-jwt": "^0.1.11",
    "@scalar/hono-api-reference": "^0.10.13",
    "hono": "^4.13.7",
    "hono-openapi": "^1.3.0"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0"
  }
}